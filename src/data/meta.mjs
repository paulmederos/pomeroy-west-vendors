// Directory-wide metadata.
//
// `lastUpdated` is when we last reviewed the list against new recommendations in
// the Pomeroy West Google Group. Bump it (YYYY-MM-DD) every time you refresh the
// directory, so neighbors can see how current it is.
export const lastUpdated = '2026-07-07';
